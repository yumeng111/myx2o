

def tx_switch(chan):
    print(f"Switching tx switch to channel {chan}")

def rx_switch(chan):
    print(f"Switching rx switch to channel {chan}")

def rx_source_switch(chan):
    print(f"Switching source to channel {chan}")

def tx_dest_switch(chan):
    print(f"Switching destination to channel {chan}")
